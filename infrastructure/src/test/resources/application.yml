spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:base_de_dados;MODE=MYSQL;DATABASE_TO_LOWER=TRUE
    username: root
    password: 123456
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false
